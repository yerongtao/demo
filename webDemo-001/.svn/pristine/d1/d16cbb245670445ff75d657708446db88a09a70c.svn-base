var class1 = require("./class")

exports.add = function(classes) {
	classes.forEach(function(item, index) {
		var _class = item
		var teacheerName = item.teacheerName
		var students = item.students
	})
	class1.add(teacheerName,students)
}